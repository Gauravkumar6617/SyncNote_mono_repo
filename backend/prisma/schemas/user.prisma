
model User{
  id String @id @default(uuid())
  username String @unique
  password String 
  Status AccountStatus @default(Pending)
  is_verified Boolen @default(False)
  email String @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}